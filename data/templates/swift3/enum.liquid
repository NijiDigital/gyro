{%- if attribute.enum_type.size > 0 %}
/*******************************************************************************
 *   DO NOT EDIT | generated 
 * - by gyro : https://github.com/NijiDigital/gyro
 * - with Liquid : https://github.com/Shopify/liquid
 *******************************************************************************/

enum {{ params.prefix }}{{ attribute.enum_type }}: String {
    {%- for value in attribute.enum_values %}
        {%- if attribute.enum_values.size > 0 %}
  case {{ value | uncapitalize }} = "{{ value | escape_quotes }}"
        {%- endif %}
    {%- endfor %}
} 
{% endif %}