{%- if attribute.enum_type.size > 0 -%}
/* DO NOT EDIT | Generated by gyro */

enum {{ params.prefix }}{{ attribute.enum_type }}: String {
{%- if attribute.enum_values.size > 0 %}
  {%- for value in attribute.enum_values %}
    case {{ value | uncapitalize }} = {% if attribute.json_values[forloop.index - 1].size > 0 -%}
        "{{ attribute.json_values[forloop.index - 1] | escape_quotes }}"
      {%- else -%}
        "{{ value | escape_quotes }}"
      {%- endif %}
  {%- endfor %}
{%- endif %}
}
{% endif %}